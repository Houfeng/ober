build:
  - name: shell
    script:
      - tsc --locale zh-CN 

dev:
  - name: shell
    script:
      - dn build

test:
  - name: lint
    source: ./src
  - name: unit

publish:
  - name: version
  - name: shell
    script:
      - dn test
      - dn build
      - npm pu --registry=http://registry.npmjs.org
  - name: submitter